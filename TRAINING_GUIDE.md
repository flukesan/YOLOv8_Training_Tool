# 🎯 คู่มือการเทรน YOLO ให้แม่นยำสูงสุด
## สำหรับงาน Go/NoGo Detection

---

## 📋 สารบัญ
1. [การเตรียมข้อมูล (Data Preparation)](#1-การเตรียมข้อมูล)
2. [การตั้งค่า Training Parameters](#2-การตั้งค่า-training-parameters)
3. [การเลือก Model](#3-การเลือก-model)
4. [Data Augmentation](#4-data-augmentation)
5. [Hardware & Performance](#5-hardware--performance)
6. [การประเมินผล และ Fine-tuning](#6-การประเมินผล-และ-fine-tuning)
7. [Common Issues & Solutions](#7-common-issues--solutions)

---

## 1. การเตรียมข้อมูล

### 1.1 ปริมาณข้อมูลที่แนะนำ

#### สำหรับ 2 Classes (Good/NG):
```
✅ แนะนำ:
├─ Training:   500-1000 ภาพต่อ class (รวม 1000-2000 ภาพ)
├─ Validation: 100-200 ภาพต่อ class (รวม 200-400 ภาพ)
└─ Test:       100-200 ภาพต่อ class (รวม 200-400 ภาพ)

สัดส่วน: Train 70% / Val 20% / Test 10%

✅ ขั้นต่ำที่ใช้งานได้:
├─ Training:   200-300 ภาพต่อ class
└─ Validation: 50-100 ภาพต่อ class

⚠️ น้อยกว่า 200 ภาพต่อ class: ควรใช้ Heavy Augmentation
```

### 1.2 คุณภาพของ Annotation

#### ✅ DO:
- **วาดกรอบให้พอดีกับวัตถุ** - ไม่เล็กหรือใหญ่เกินไป
- **ครอบคลุมทุกมุมของวัตถุ** - รวมส่วนที่อาจมีตำหนิ
- **สม่ำเสมอในการวาด** - ให้คนเดียวกันทำทั้งหมด หรือมี guideline ชัดเจน
- **ตรวจสอบความถูกต้อง** - Review annotation อย่างน้อย 2 รอบ
- **Label ทุกวัตถุในภาพ** - อย่าลืม object ที่อยู่ชิดขอบหรือบางส่วน

#### ❌ DON'T:
- กรอบหลุดออกนอกวัตถุมากเกินไป
- กรอบเล็กเกินไป (ไม่ครอบคลุมพื้นที่ตำหนิ)
- ลืม annotate บางวัตถุ
- Annotation ไม่สอดคล้องกัน (บางภาพวาดแน่น บางภาพวาดหลวม)
- Label ผิด class

### 1.3 ความหลากหลายของข้อมูล (Data Diversity)

#### 📸 มุมกล้อง:
```
✅ ควรมี:
├─ มุมตรง (90°)      - มุมมองมาตรฐาน
├─ มุมเฉียง (45°)    - มุมมองเฉียง
├─ มุมสูง (Bird eye) - มองจากด้านบน
└─ มุมต่ำ (Low angle)- มองจากด้านล่าง (ถ้าใช้งานจริงมี)
```

#### 💡 แสงสว่าง:
```
✅ ควรมี:
├─ แสงสว่างมาตรฐาน
├─ แสงสว่างเกิน (Overexposed)
├─ แสงสว่างน้อย (Underexposed)
├─ แสงส่องจากด้านหลัง (Backlight)
├─ เงาบนวัตถุ (Shadows)
└─ แสงสะท้อน (Reflections) - สำหรับวัตถุมันเงา
```

#### 📏 ขนาดและระยะ:
```
✅ ควรมี:
├─ ระยะใกล้  (วัตถุเต็มภาพ)
├─ ระยะกลาง  (วัตถุครึ่งภาพ)
└─ ระยะไกล   (วัตถุเล็ก 10-20% ของภาพ)
```

#### 🎨 พื้นหลัง:
```
✅ ควรมี:
├─ พื้นหลังเรียบ (สีเดียว)
├─ พื้นหลังที่มี texture
├─ พื้นหลังที่มีวัตถุอื่นแทรก
└─ พื้นหลังจริงในสภาพการผลิต
```

#### ⚖️ Class Balance:
```
✅ อัตราส่วนที่ดี:
├─ Good : NG = 1:1 (ดีที่สุด)
├─ Good : NG = 2:1 หรือ 1:2 (ยอมรับได้)
└─ Good : NG > 3:1 หรือ < 1:3 (ควร re-balance)

💡 ถ้า imbalance มาก:
- ใช้ class_weight
- Oversample class ที่น้อย
- Undersample class ที่เยอะ
```

### 1.4 การตรวจสอบคุณภาพข้อมูล

```bash
✅ Checklist:
□ ทุกภาพมี annotation
□ Annotation ถูก class
□ กรอบไม่หลุดออกนอกวัตถุ
□ ไม่มีภาพซ้ำ (duplicates)
□ ไม่มีภาพเบลอหรือพร่ามากเกินไป
□ ปริมาณแต่ละ class สมดุล
□ มีความหลากหลายของมุมกล้อง/แสง/ระยะ
□ แยก train/val/test แล้ว
```

---

## 2. การตั้งค่า Training Parameters

### 2.1 Parameters สำหรับข้อมูลน้อย (< 1000 ภาพ)

```python
# สำหรับข้อมูล 200-1000 ภาพ
epochs: 200-300         # เทรนนานขึ้นเพื่อเรียนรู้จากข้อมูลน้อย
batch: 8-16            # ลดขนาด batch (ป้องกัน overfitting)
imgsz: 640             # ขนาดมาตรฐาน
patience: 50-100       # Early stopping (อดทนมากขึ้น)
lr0: 0.001-0.005      # Learning rate ต่ำ (เรียนรู้ช้าแต่แม่นยำ)
weight_decay: 0.0005   # Regularization เพื่อลด overfitting

# Augmentation: ใช้ 'heavy' preset
```

### 2.2 Parameters สำหรับข้อมูลปานกลาง (1000-5000 ภาพ)

```python
# สำหรับข้อมูล 1000-5000 ภาพ (แนะนำสำหรับ Go/NoGo)
epochs: 150-200
batch: 16-32
imgsz: 640
patience: 50
lr0: 0.01
lrf: 0.01
momentum: 0.937
weight_decay: 0.0005
optimizer: 'SGD'       # หรือ 'Adam' สำหรับการเรียนรู้เร็วขึ้น
cos_lr: False          # หรือ True สำหรับ learning rate scheduling
amp: True              # Automatic Mixed Precision (เร็วขึ้น)

# Augmentation: ใช้ 'medium' หรือ 'industrial' preset
```

### 2.3 Parameters สำหรับข้อมูลเยอะ (> 5000 ภาพ)

```python
# สำหรับข้อมูล > 5000 ภาพ
epochs: 100-150
batch: 32-64          # เพิ่ม batch size (เร็วขึ้น)
imgsz: 640
patience: 50
lr0: 0.01
optimizer: 'SGD'
workers: 8            # เพิ่ม workers สำหรับโหลดข้อมูลเร็วขึ้น

# Augmentation: ใช้ 'light' หรือ 'medium' preset
```

### 2.4 Optimizer แต่ละตัว

```python
'SGD'      # มาตรฐาน, stable, แนะนำสำหรับ production
'Adam'     # เรียนรู้เร็ว, ดีสำหรับ small dataset
'AdamW'    # Adam + weight decay, ลด overfitting ได้ดี
'RMSprop'  # เรียนรู้เร็ว, unstable บ้าง
'LION'     # ใหม่, efficient, ต้องลองทดสอบ
```

---

## 3. การเลือก Model

### 3.1 Model Comparison

| Model    | Speed | mAP  | Params | Size  | Use Case |
|----------|-------|------|--------|-------|----------|
| YOLOv8n  | ⚡⚡⚡⚡⚡ | 37.3 | 3.2M   | 6.2MB | Real-time, Edge devices |
| YOLOv8s  | ⚡⚡⚡⚡  | 44.9 | 11.2M  | 21.5MB| **Recommended for Go/NoGo** |
| YOLOv8m  | ⚡⚡⚡   | 50.2 | 25.9M  | 49.7MB| High accuracy needed |
| YOLOv8l  | ⚡⚡    | 52.9 | 43.7M  | 83.7MB| Maximum accuracy |
| YOLOv8x  | ⚡     | 53.9 | 68.2M  | 130.5MB| Best accuracy, slow |

### 3.2 เลือก Model ตาม Use Case

#### สำหรับ Go/NoGo Detection:

```
📦 สินค้าขนาดใหญ่ (> 50% ของภาพ):
└─ YOLOv8s หรือ YOLOv8n

📦 สินค้าขนาดกลาง (20-50% ของภาพ):
└─ YOLOv8s (แนะนำ) หรือ YOLOv8m

📦 สินค้าขนาดเล็ก (< 20% ของภาพ):
└─ YOLOv8m หรือ YOLOv8l

🔍 ตำหนิเล็กมาก (< 5% ของวัตถุ):
└─ YOLOv8l หรือ YOLOv8x + เพิ่ม imgsz: 1280

⚡ ต้องการ Real-time (> 30 FPS):
└─ YOLOv8n หรือ YOLOv8s + TensorRT optimization

🎯 ต้องการความแม่นยำสูงสุด (ไม่สนใจความเร็ว):
└─ YOLOv8x + imgsz: 1280
```

### 3.3 Progressive Training Strategy

```
Step 1: เริ่มต้นด้วย YOLOv8s
├─ เทรน 100 epochs
├─ ประเมินผล (mAP, Precision, Recall)
└─ ถ้าไม่พอใจ → ไป Step 2

Step 2: ลองปรับ Parameters
├─ เพิ่ม epochs เป็น 200
├─ ลด learning rate เหลือ 0.005
├─ เพิ่ม augmentation
└─ ถ้ายังไม่พอใจ → ไป Step 3

Step 3: อัพเกรด Model
├─ เปลี่ยนเป็น YOLOv8m
├─ เทรน 150 epochs
└─ ถ้ายังไม่พอใจ → ไป Step 4

Step 4: Maximum Accuracy
├─ เปลี่ยนเป็น YOLOv8l หรือ YOLOv8x
├─ เพิ่ม imgsz: 1280
└─ เทรน 200 epochs
```

---

## 4. Data Augmentation

### 4.1 Augmentation Presets

โปรแกรมมี 4 presets ให้เลือก (ใน `config/settings.py`):

#### 🟢 Light Augmentation
สำหรับ: สภาพแวดล้อมควบคุมได้ดี, ข้อมูลเยอะ (> 3000 ภาพ)
```python
'hsv_h': 0.01      # เปลี่ยนสีเล็กน้อย
'hsv_s': 0.5       # เปลี่ยน saturation เล็กน้อย
'hsv_v': 0.3       # เปลี่ยนความสว่างเล็กน้อย
'degrees': 5.0     # หมุนได้ ±5 องศา
'translate': 0.05  # เลื่อนตำแหน่ง ±5%
'scale': 0.3       # ซูมได้ ±30%
'fliplr': 0.5      # พลิกซ้าย-ขวา 50%
'mosaic': 0.5      # รวมภาพ 50%
```

#### 🟡 Medium Augmentation (Default)
สำหรับ: ใช้ทั่วไป, ข้อมูลปานกลาง (1000-3000 ภาพ)
```python
'hsv_h': 0.015
'hsv_s': 0.7
'hsv_v': 0.4
'degrees': 10.0    # หมุนได้ ±10 องศา
'translate': 0.1   # เลื่อนตำแหน่ง ±10%
'scale': 0.5       # ซูมได้ ±50%
'fliplr': 0.5
'mosaic': 1.0      # รวมภาพ 100%
'mixup': 0.1       # ผสมภาพ 10%
```

#### 🔴 Heavy Augmentation
สำหรับ: ข้อมูลน้อย (< 1000 ภาพ), สภาพแวดล้อมหลากหลาย
```python
'hsv_h': 0.02
'hsv_s': 0.8
'hsv_v': 0.5
'degrees': 15.0    # หมุนได้ ±15 องศา
'translate': 0.15
'scale': 0.7
'shear': 5.0       # บิดเบือนภาพ
'perspective': 0.0005
'fliplr': 0.5
'flipud': 0.2      # พลิกบน-ล่าง 20%
'mosaic': 1.0
'mixup': 0.2
'copy_paste': 0.1
```

#### 🏭 Industrial Preset (แนะนำสำหรับ Go/NoGo!)
สำหรับ: งาน Industrial Inspection, Quality Control
```python
'hsv_h': 0.01      # เปลี่ยนสีน้อย (สินค้ามีสีแน่นอน)
'hsv_s': 0.6
'hsv_v': 0.5       # จำลองแสงต่างๆ
'degrees': 10.0    # สินค้าอาจหมุนเล็กน้อย
'translate': 0.1   # ตำแหน่งเปลี่ยนได้
'scale': 0.4       # ขนาดเปลี่ยนได้เล็กน้อย
'fliplr': 0.5      # พลิกซ้าย-ขวาได้
'flipud': 0.0      # ไม่พลิกบน-ล่าง (สินค้าไม่กลับหัว)
'mosaic': 0.8      # รวมภาพหลายชิ้น
'mixup': 0.0       # ไม่ผสมภาพ (จะทำให้ตำหนิเบลอ)
'copy_paste': 0.0  # ไม่ copy-paste (จะทำให้สับสน)
```

### 4.2 วิธีใช้ Augmentation Preset

ใน code สามารถใช้ได้ดังนี้:
```python
from config.settings import Settings

# เลือก preset
aug_params = Settings.AUGMENTATION_PRESETS['industrial']

# รวมกับ training params
train_config = {**Settings.DEFAULT_TRAIN_PARAMS, **aug_params}

# เทรน
model.train(**train_config, data='data.yaml')
```

### 4.3 คำแนะนำเลือก Augmentation

```
ข้อมูล < 500 ภาพ     → Heavy
ข้อมูล 500-2000 ภาพ  → Medium หรือ Industrial
ข้อมูล > 2000 ภาพ    → Light หรือ Industrial

สภาพแวดล้อมควบคุมได้ (ห้องแล็ป) → Light
สภาพแวดล้อมปานกลาง               → Medium
สภาพแวดล้อมหลากหลาย (outdoor)    → Heavy

งาน Industrial QC → Industrial (Recommended!)
```

---

## 5. Hardware & Performance

### 5.1 Hardware Requirements

#### สำหรับ Training:

```
Minimum (YOLOv8s, batch=8):
├─ GPU: GTX 1060 6GB หรือเทียบเท่า
├─ RAM: 16 GB
├─ Storage: 50 GB SSD
└─ Time: ~2-4 ชั่วโมงต่อ 100 epochs

Recommended (YOLOv8m, batch=16):
├─ GPU: RTX 3060 12GB หรือ RTX 3070
├─ RAM: 32 GB
├─ Storage: 100 GB SSD
└─ Time: ~1-2 ชั่วโมงต่อ 100 epochs

Optimal (YOLOv8l, batch=32):
├─ GPU: RTX 4090 24GB หรือ A5000
├─ RAM: 64 GB
├─ Storage: 200 GB NVMe SSD
└─ Time: ~30-60 นาทีต่อ 100 epochs
```

#### สำหรับ Inference:

```
Edge Device (Jetson Nano):
└─ YOLOv8n + TensorRT (10-15 FPS @ 640x640)

PC (GTX 1060):
└─ YOLOv8s (30-40 FPS @ 640x640)

Workstation (RTX 3090):
└─ YOLOv8x (60+ FPS @ 640x640)
```

### 5.2 Performance Optimization

#### 1. Mixed Precision Training (AMP)
```python
amp: True  # ลดการใช้ VRAM 40-50%, เร็วขึ้น 2x
```

#### 2. Caching
```python
cache: 'ram'   # โหลดข้อมูลลง RAM (เร็วขึ้น 2-3x)
cache: 'disk'  # เก็บ cached images บน disk
cache: None    # ไม่ cache (ประหยัด RAM)
```

#### 3. Workers
```python
workers: 8     # CPU cores สำหรับโหลดข้อมูล
               # แนะนำ: จำนวน CPU cores / 2
```

#### 4. Multi-GPU
```python
device: '0,1'  # ใช้ GPU 0 และ 1
device: '0,1,2,3'  # ใช้ 4 GPUs
```

#### 5. Batch Size Optimization
```python
# หา optimal batch size:
batch: -1      # Auto-detect optimal batch size
```

---

## 6. การประเมินผล และ Fine-tuning

### 6.1 Metrics ที่ต้องดู

#### Precision (ความแม่นยำในการทำนาย)
```
Precision = TP / (TP + FP)

สูง = ทุกครั้งที่บอกว่า NG จะเป็น NG จริงๆ
ต่ำ = บอกว่า NG แต่จริงๆ เป็น Good (False Positive เยอะ)

เป้าหมาย: > 0.90 (90%)
```

#### Recall (ความครอบคลุมในการตรวจจับ)
```
Recall = TP / (TP + FN)

สูง = จับ NG ได้ครบทุกอัน
ต่ำ = มี NG หลุดไป (False Negative เยอะ)

เป้าหมาย: > 0.90 (90%)
```

#### mAP50 (Mean Average Precision @ IoU=0.50)
```
ความแม่นยำโดยรวมที่ IoU threshold = 0.5

เป้าหมาย: > 0.85 (85%)
```

#### mAP50-95 (mAP @ IoU=0.50-0.95)
```
ความแม่นยำโดยรวมที่ IoU threshold = 0.5-0.95

เป้าหมาย: > 0.70 (70%)
```

### 6.2 เป้าหมายสำหรับ Go/NoGo Detection

```
🎯 Acceptable (ใช้งานได้):
├─ Precision: > 0.85
├─ Recall: > 0.85
├─ mAP50: > 0.80
└─ mAP50-95: > 0.65

🎯 Good (ดี):
├─ Precision: > 0.90
├─ Recall: > 0.90
├─ mAP50: > 0.90
└─ mAP50-95: > 0.75

🎯 Excellent (ยอดเยี่ยม):
├─ Precision: > 0.95
├─ Recall: > 0.95
├─ mAP50: > 0.95
└─ mAP50-95: > 0.85
```

### 6.3 Fine-tuning Strategy

#### ถ้า Precision ต่ำ (False Positive เยอะ):
```
1. เพิ่ม confidence threshold (0.25 → 0.5)
2. ตรวจสอบ annotation - Good ถูก label เป็น NG หรือไม่
3. เพิ่มข้อมูล Good ที่หลากหลาย
4. ลด augmentation (เช่น mixup, copy_paste)
5. เพิ่ม epochs
```

#### ถ้า Recall ต่ำ (False Negative เยอะ):
```
1. ลด confidence threshold (0.25 → 0.1)
2. ตรวจสอบ annotation - มี NG ที่ลืม label หรือไม่
3. เพิ่มข้อมูล NG ที่หลากหลาย (โดยเฉพาะตำหนิเล็กๆ)
4. อัพเกรด model (s → m → l)
5. เพิ่ม imgsz (640 → 1280)
```

#### ถ้า Overfitting (train ดีแต่ val แย่):
```
1. เพิ่ม augmentation
2. เพิ่ม weight_decay
3. ลด model size (m → s → n)
4. ลด epochs
5. เพิ่มข้อมูล validation
6. ใช้ Early Stopping (patience: 50)
```

#### ถ้า Underfitting (ทั้ง train และ val แย่):
```
1. อัพเกรด model (s → m → l)
2. เพิ่ม epochs
3. เพิ่ม learning rate
4. ลด augmentation
5. ตรวจสอบคุณภาพข้อมูล
6. เพิ่มปริมาณข้อมูล
```

---

## 7. Common Issues & Solutions

### 7.1 "loss nan" หรือ Training ระเบิด

**สาเหตุ:**
- Learning rate สูงเกินไป
- Batch size เล็กเกินไป
- Annotation ผิดพลาด (กรอบหลุดนอกภาพ)

**แก้ไข:**
```python
lr0: 0.001         # ลด learning rate
batch: 16          # เพิ่ม batch size
# ตรวจสอบ annotation ทุกภาพ
```

### 7.2 CUDA Out of Memory

**แก้ไข:**
```python
batch: -1          # Auto-detect
# หรือ
batch: 8           # ลด batch size
imgsz: 416         # ลดขนาดภาพ (จาก 640)
cache: None        # ปิด caching
workers: 4         # ลด workers
```

### 7.3 Training ช้ามาก

**แก้ไข:**
```python
amp: True          # เปิด Mixed Precision
cache: 'ram'       # Cache ข้อมูลใน RAM
workers: 8         # เพิ่ม workers (= CPU cores / 2)
device: '0'        # ตรวจสอบใช้ GPU
batch: 32          # เพิ่ม batch size (ถ้า VRAM พอ)
```

### 7.4 Validation mAP ไม่เพิ่มขึ้น (Plateau)

**แก้ไข:**
1. ตรวจสอบ Learning Rate Schedule
```python
cos_lr: True       # ใช้ Cosine LR
lrf: 0.01          # Final LR factor
```

2. ใช้ Early Stopping
```python
patience: 50       # หยุดถ้า 50 epochs ไม่ดีขึ้น
```

3. Fine-tune จาก Best Checkpoint
```python
model = YOLO('runs/train/exp/weights/best.pt')
model.train(epochs=100, lr0=0.001)  # เทรนต่อด้วย LR ต่ำ
```

### 7.5 Model ตรวจจับไม่ได้ (mAP = 0)

**ตรวจสอบ:**
1. Annotation format ถูกต้องหรือไม่ (YOLO format)
2. Class ID ใน annotation ตรงกับ data.yaml หรือไม่
3. ไฟล์ภาพและ label ชื่อตรงกันหรือไม่
4. data.yaml มี path ถูกต้องหรือไม่

### 7.6 Inference ช้า

**แก้ไข:**
1. Export เป็น TensorRT (เร็วขึ้น 5-10x)
```python
model.export(format='engine', half=True)
```

2. ใช้ Model เล็กลง (m → s → n)

3. ลดขนาดภาพ
```python
results = model.predict(img, imgsz=416)
```

---

## 📝 Training Checklist

ก่อนเทรน:
```
□ เตรียมข้อมูลอย่างน้อย 500 ภาพต่อ class
□ ตรวจสอบ annotation ทุกภาพ
□ แยก train/val/test (70/20/10)
□ Class balance (Good:NG ใกล้เคียง 1:1)
□ ความหลากหลาย (มุม/แสง/ระยะ)
□ เลือก model ที่เหมาะสม
□ เลือก augmentation preset
□ ตั้งค่า parameters ที่เหมาะสม
```

ระหว่างเทรน:
```
□ ดู loss กราฟ (ควรลดลงเรื่อยๆ)
□ ดู mAP (ควรเพิ่มขึ้นเรื่อยๆ)
□ ตรวจสอบ validation predictions
□ Monitor GPU usage
□ Check for overfitting
```

หลังเทรน:
```
□ ประเมิน metrics (Precision, Recall, mAP)
□ ทดสอบกับ test set
□ ทดสอบกับข้อมูลจริง
□ Fine-tune ถ้าจำเป็น
□ Export model
□ Optimize สำหรับ production
```

---

## 🎓 สรุป - Tips สำคัญ

1. **Data is King** - ข้อมูลดีสำคัญที่สุด (70% ของความสำเร็จ)
2. **Start Small** - เริ่มจาก YOLOv8s ก่อน แล้วค่อยอัพเกรด
3. **Monitor Everything** - ดู metrics ทุกตัว อย่าดูแค่ mAP
4. **Use Augmentation** - สำคัญมากสำหรับข้อมูลน้อย
5. **Be Patient** - ถ้า Early Stopping เร็ว ให้เพิ่ม patience
6. **Test on Real Data** - ทดสอบกับข้อมูลจริงในสภาพการใช้งาน
7. **Iterate** - เทรน → ประเมิน → ปรับปรุง → ทำซ้ำ

---

**Good luck with your training! 🚀**

สำหรับคำถามหรือปัญหา กรุณาดูที่ Common Issues section หรือติดต่อทีม support
