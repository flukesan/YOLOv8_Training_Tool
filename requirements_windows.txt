# YOLOv8 and Computer Vision (Windows-friendly versions)
ultralytics>=8.0.0
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
Pillow>=10.0.0

# Deep Learning (with pre-built wheels)
# --------------------------------------------------------------------------
# IMPORTANT (Windows): the default PyPI torch wheel is CPU-ONLY, so the GPU
# will NOT be detected. For an NVIDIA GPU, install the CUDA build instead:
#
#   pip uninstall -y torch torchvision
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# (cu124 = CUDA 12.x; use cu118 for older drivers - match your `nvidia-smi`.)
# Verify with:  python -c "import torch; print(torch.cuda.is_available())"
# --------------------------------------------------------------------------
torch>=2.0.0
torchvision>=0.15.0

# GUI Framework
PyQt6>=6.5.0

# Data Processing (avoid packages requiring C++ compiler)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
PyYAML>=6.0
tqdm>=4.65.0
python-dotenv>=1.0.0
psutil>=5.9.0
py-cpuinfo>=9.0.0
